# Port Configuration
# The main application port (change this to change the port you access the app on)
APP_PORT=86

# Database Configuration (Docker Internal)
DB_PASSWORD=Password123!
DB_DATABASE=QC_Tool_DB
DB_PORT=1433

# Connection String for the Backend
# Use this one for the Dockerized SQL Server (Default)
DB_CONNECTION_STRING="Server=database;Database=QC_Tool_DB;User Id=sa;Password=Password123!;Encrypt=False;TrustServerCertificate=True;MultipleActiveResultSets=true;"

# API Configuration (should match APP_PORT)
NEXT_PUBLIC_API_URL=http://localhost:${APP_PORT}/api
